<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tech Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<header>
    <div class="container-fluid w-100 bg-info">
        <h1 class="text-left display-4 p-4">Tech Blog</h1>
        <nav class="navbar navbar-expand-lg navbar-light bg-light p-3">
            {{#if loggedIn}}
            <a class="navbar-brand" href="/">Home</a>
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="#">Create Post<span class="sr-only"></span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/api/post">Browse Posts</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Logout</a>
                </li>
                {{else}}
                <a href="/" class="navbar-item is-hoverable pt-1">homepage</a>
                <a href="/login" class="button is-primary">login</a>
                {{/if}}
            </ul>
            <form class="form-inline d-flex flex-row my-2 my-lg-0">
                <input class="form-control mr-sm-2 p-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0 d-inline-flex" type="submit">Search</button>
            </form>
        </nav>
    </div>
</header>

<main class="container">
    <h1 class="display-2 py-5">Hello, User</h1>
    {{{ body }}}
</main>

<footer class="footer rows p-4 text-center">
    <div class="row-is-full content has-text-centered">
        <p>
            <a href="/">homepage</a>{{#if loggedIn}} |
            <a href="/dashboard/">dashboard</a> |
            <a href="/dashboard/create/">create new post</a>
            {{else}}
            | <a href="/login/">login</a>
            {{/if}}
        </p>
    </div>
    <div class="row-is-full content has-text-centered">
        <p>
            <strong>A tech blog</strong> by<a href="https://ahooper00@github.com"> Amelia Hooper</a>
        </p>
    </div>
</footer>